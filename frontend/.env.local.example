# URL de l'API backend (accessible depuis le serveur Next.js et le navigateur pour les routes publiques)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Clé API — côté serveur uniquement (server components, route handlers)
# Ne JAMAIS préfixer par NEXT_PUBLIC_
INTERNAL_API_KEY=dev-api-key-changeme
